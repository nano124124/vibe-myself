# 2026-03-30 작업 기록

## 완료
- 어드민 카테고리 관리 화면 구현 (`/admin/goods/categories`)
  - 좌측 카테고리 트리 테이블 + 우측 인라인 패널 구조
  - TDD: goods.api, useCategoryList/Create/Update, CategoryTree, CategoryPanel (총 15개 테스트)
  - `frontend/types/goods.types.ts` 위치 오류 수정 (`src/types/` → `types/`)
- `CategoryMapper.xml` keyColumn 대소문자 버그 수정 (`CTG_NO` → `ctg_no`)
- `ST_MENU_BASE` 카테고리관리 메뉴 항목 추가
- `feature/category` → `master` 머지 및 푸시
- CLAUDE.md 협업 규칙에 `--no-ff` 머지 정책 추가

## 진행 중
- 없음

## 다음 작업
- 미정

## 메모
- 머지는 앞으로 `git merge --no-ff`로 진행 (CLAUDE.md에 규칙 추가됨)
- PostgreSQL은 따옴표 없이 생성된 컬럼을 소문자로 저장 → MyBatis keyColumn은 소문자로 작성할 것
- 카테고리/옵션 샘플 데이터 SQL 준비됨 (대화 내 제공, 아직 DB 적용 안 함)
