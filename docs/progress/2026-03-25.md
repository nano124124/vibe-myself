# 2026-03-25 작업 기록

## 완료
- Auth 설계 문서 작성 (`docs/superpowers/specs/2026-03-25-auth-design.md`)
- Auth 구현 플랜 작성 (`docs/superpowers/plans/2026-03-25-auth.md`)
- `feature/auth` 브랜치에 JWT + Redis 인증 전체 구현 (22 tasks, 38 tests)
  - `common/` — JwtProvider, JwtAuthFilter, RedisService, LoginUser, CookieUtils
  - `global/` — BusinessException, UnauthorizedException, GlobalExceptionHandler
  - `config/` — SecurityConfig (STATELESS, URL 권한), PasswordConfig
  - `member/` — Member, MemberMapper, LoginMemberRequest, MemberAuthService, MemberAuthController
  - `system/` — Admin, AdminMapper, LoginAdminRequest, AdminAuthService, AdminAuthController
  - 테스트 — `@WithMockMember`, `@WithMockAdmin` 어노테이션
- `feature/auth` 브랜치 remote push 완료

## 진행 중
- 없음

## 다음 작업
- `feature/auth` → `master` PR/머지
- `application.yml`에서 DB 비밀번호 제거 → `application-local.yml`로 이동 (보안 이슈)
- 프론트엔드 로그인 화면 연동

## 메모
- `application.yml`에 DB credentials 평문 포함 상태로 커밋됨 — 다음 세션에서 반드시 제거
- `application-local.yml`은 gitignore 처리되어 있음 (로컬에만 존재)
- JWT secret은 `application-local.yml`의 `jwt.secret`으로 관리
- Java 21 경로: `/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`
- worktree: `.worktrees/feature/auth`
