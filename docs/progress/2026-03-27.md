# 2026-03-27 작업 기록

## 완료
- `proxy.ts` jose JWT 서명 검증 적용, 어드민 미들웨어 활성화
  - `authGuard.ts`: `atob()` 디코딩 → `jose` `jwtVerify` 서명 검증으로 교체
  - `proxy.ts`: `async` + `await` 적용 → Next.js 16 미들웨어 정상 활성화
  - 비어드민 role 리다이렉트 `/admin` → `/` 변경 (무한루프 방지)
  - `payload.role` 런타임 타입 체크 추가 (`typeof role !== 'string'`)
  - 단위 테스트 13개 추가 (`lib/__tests__/authGuard.test.ts`)
  - `jose` 패키지 추가, `JWT_SECRET` 환경변수 `.env.local`에 추가

## 진행 중
- 없음

## 다음 작업
- 카테고리 관리 화면 개발 (`goods` 모듈, 어드민)
  - 카테고리 목록 (`/admin/goods/categories`)
  - 카테고리 등록 화면
  - 카테고리 수정 화면

## 메모
- Next.js 16에서 미들웨어 파일 관례가 `middleware.ts` → `proxy.ts`로 변경됨 (breaking change)
  - 함수명도 `middleware` → `proxy`로 변경
  - 빌드 출력: `ƒ Proxy (Middleware)` 로 정상 인식 확인
- `jose`의 `jwtVerify`는 Edge Runtime 호환. `atob()`으로 Base64 시크릿 디코딩
- SSO 구조 전환은 핵심 기능(상품/주문/프로모션) 구현 후 별도 브랜치로 진행 예정
