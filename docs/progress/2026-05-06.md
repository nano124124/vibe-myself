# 2026-05-06 작업 기록

## 완료
- `app/(shop)/products/`, `app/admin/(main)/products/` → `goods/`로 폴더 리네임
- 어드민 상품 목록 조회 구현 (`feature/goods-list` 브랜치)
  - Backend: `GET /api/admin/goods` API (검색: 상품명·판매상태·카테고리, offset 페이지네이션)
    - `GoodsListRequest`, `GoodsListItemResponse` DTO 추가
    - `PageResponse<T>` 공통 응답 래퍼 추가 (`common/response/`)
    - `GoodsMapper` — `selectGoodsList`, `countGoodsList` 추가
    - `GoodsListService` + 단위 테스트 3개
    - `GoodsController` — GET 엔드포인트 추가
  - Frontend: 어드민 상품 목록 페이지 (`/admin/goods`)
    - `types/goods.types.ts` — `GoodsListItemResponse`, `GoodsListSearchParams`, `PageResponse<T>` 추가
    - `api/goods.api.ts` — `getAdminGoodsList` 추가
    - `hooks/goods/useAdminGoodsList.ts` 신규
    - `components/goods/GoodsListTable.tsx` 신규
    - `app/admin/(main)/goods/page.tsx` 구현
- `CLAUDE.md` 협업 규칙 보강 — `form-validation-tests` 스킬 호출 시 폼 여부 스스로 판단 금지 명시

## 진행 중
- 없음

## 다음 작업
- `/admin/goods/[id]` — 어드민 상품 상세/수정 화면
- `/goods/[id]` — 고객용 상품 상세 화면
- `feature/goods-list` → `master` 머지

## 메모
- `CategoryServiceTest` 4개 실패는 기존 문제 (`SecurityUtils.currentUserId()` NPE) — 별도 수정 필요
- `GoodsMapper`에 조회/CUD 혼재 중 → 규모 커지면 `GoodsQueryMapper` / `GoodsCommandMapper` 분리 고려
- `products` 폴더가 `goods` 안에 중첩 생성되는 이슈 발생 (이미 존재하는 디렉토리에 `mv` 시 내부로 이동) → 수동 삭제 완료