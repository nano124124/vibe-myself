# 2026-04-07 작업 기록

## 완료

### 상품 등록 - 옵션 토글 + 이미지 파일 업로드 (Task 5~8)
- `GoodsController`: `@RequestPart` multipart/form-data 방식으로 전환
- `GoodsCreateService`: `SupabaseStorageService` 연동, 옵션 없음(빈 배열) 지원
- `CodeController`: `ApiResponse` 패키지 및 응답 포맷 통일
- `GoodsImageInput`: URL 직접 입력 → 파일 업로드 UI (썸네일 미리보기, 대표 뱃지)
- `create/page.tsx`: `imgFiles` state, 옵션 없음/사용 라디오버튼 추가
- `useGoodsCreate`: mutationFn 시그니처 `{ data, images }` 로 변경
- `goods.api.ts`: `createGoods` multipart FormData 전송으로 변경
- `goods.types.ts`: `CreateGoodsRequest`에서 `imgUrls` 제거
- `layout.tsx`: `Toaster` 추가
- `system.types.ts`: `CODE_GROUP` 값 수정 (`GOODS_TP_CD`, `SALE_STAT_CD`)

### goods 도메인 정책 문서 (`docs/domain-policy/goods.md`)
- 구조: 개념 정의 → 불변조건 → 상태 전이 → 오퍼레이션 정책 → 도메인 경계 → 미결 정책
- 판매상태를 `SELLING` / `SOLD_OUT` / `STOPPED` 3단계로 정의
  - `SOLD_OUT`은 관리자가 직접 설정하지 않고 재고 변경 시 시스템이 재계산
- 전시 상태(`DISPLAY_ON` / `DISPLAY_OFF`) 별도 정의
- 도메인 경계 명시: 재고 차감(order), 최종 가격 계산(prom), 장바구니 처리(cart)
- 단품 비활성화/상품 STOPPED 시 cart·wishlist 처리 결정:
  - 레코드는 유지, 조회 시 필터링은 cart 도메인 책임

## 진행 중
- 없음

## 다음 작업
- 상품 등록 실제 연동 테스트 (백엔드 서버 붙여서 동작 확인)
- `GoodsSaleStatus` enum을 `SELLING` / `SOLD_OUT` / `STOPPED` 3단계로 변경 (도메인 정책 정합성)
- 어드민 상품 목록 조회 화면 (`/admin/goods`)
- 고객용 상품 상세 화면 (`/(shop)/goods/[id]`)

## 메모
- `docs/domain-policy/` 하위에 도메인별 정책 문서 관리 시작 (goods.md 첫 번째)
- 미결 정책 3개 남아있음: 판매가 변경 적용 시점, 판매종료일시 자동 전이, 프로모션 연계 가격 정책
- git commit 시 `heredoc` 방식이 index.lock 충돌 일으킴 → `/tmp/commit_msg.txt` 파일 방식으로 우회
