# 2026-04-17 작업 기록

## 완료
- `middleware.ts` 삭제 — `proxy.ts`와 충돌하던 구 컨벤션 파일 제거
- 상품 등록 E2E 테스트 보강 (`e2e/goods/goods-create.spec.ts`)
  - 401 인증 오류 케이스 추가
  - 마진율 표시 케이스 2개 추가 (정상 계산 / 공급원가 미입력)
  - 음수 마진율(공급원가 > 판매가) 제출 차단 케이스 추가
- 음수 마진율 유효성 검증 구현
  - Frontend: `setError('suplyPrc', ...)` 로 제출 차단 + `GoodsBasicInfoForm`에 에러 표시 추가
  - Backend: `ErrorCode.NEGATIVE_MARGIN_RATE` 추가, `GoodsCreateService.validate()` 에 검증 로직 추가
- `GoodsCreateServiceTest.java` 생성 — `calcMrgnRate` 단위 테스트 5개 + 음수 마진율 검증 2개
- `SupabaseStorageService` 버그 수정
  - `delete()` → `method(HttpMethod.DELETE)` 로 변경 (WebClient body 설정 불가 문제)
  - `goodsNo + "/" + filename` → `String.join("/", goodsNo, filename)` (SonarLint S1075)
- `SupabaseStorageServiceTest` 생성자 시그니처 수정 (`SupabaseProperties` record 사용)

## 진행 중
- 없음

## 다음 작업
- 어드민 상품 목록 조회 화면 (`/admin/goods`)
- 고객용 상품 상세 화면 (`/goods/[id]`)

## 메모
- `CategoryServiceTest` 4개 실패는 기존 문제 (`SecurityUtils.currentUserId()` NPE) — 이번 작업과 무관, 별도 수정 필요
- `calcMrgnRate`는 package-private으로 변경하여 직접 테스트 가능