# 2026-04-06 작업 기록

## 완료
- `GoodsBasicInfoForm` form-validation-tests 스킬 실행 완료
  - Vitest 단위 테스트 10개 (`GoodsBasicInfoForm.test.tsx`)
  - Playwright E2E 테스트 3개 (`e2e/goods/goods-create.spec.ts`)
  - 테스트 문서 생성 (`docs/testing/goods/goods-basic-info-form.md`)
- `vitest.config.ts` — `e2e/` 폴더 exclude 추가 (Playwright spec 파일 충돌 방지)
- `e2e/system/admin-login.spec.ts` — fake JWT → 실제 서명 JWT로 교체 (authGuard jwtVerify 통과)
- PostToolUse 훅 개선: `Form` 파일 한정 → `frontend/components/` 하위 모든 `.tsx` 감지
- `CLAUDE.md` 협업 규칙에 컴포넌트 수정 후 `form-validation-tests` 스킬 강제 실행 규칙 추가
- `session-wrap-up` 스킬을 글로벌(`~/.claude`)에서 프로젝트(`.claude/skills/`)로 이동 (다른 PC 공유 목적)

## 진행 중
- 없음

## 다음 작업
- 상품 등록 API 실제 연동 테스트 (백엔드 서버 붙여서 동작 확인)
- 어드민 상품 목록 조회 화면 (`/admin/goods`)
- 고객용 상품 상세 화면 (`/goods/[id]`)

## 메모
- E2E 테스트의 JWT는 `.env.local`의 `JWT_SECRET`으로 서명 필요 — 다른 PC에서 작업 시 `.env.local` 먼저 세팅할 것
- PostToolUse 훅은 `.claude/settings.json`에 있어 git으로 공유됨
- Playwright 브라우저 미설치 시 `pnpm exec playwright install chromium` 실행 필요
