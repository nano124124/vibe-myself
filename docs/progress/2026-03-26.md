# 2026-03-26 작업 기록

## 완료
- `lib/api.ts` baseURL 제거 — Next.js 프록시 경유하도록 수정 (CORS 해결)
- `next.config.ts` rewrite destination에 `/api` 추가 — Spring 컨트롤러 경로 불일치(403) 수정
- `GlobalExceptionHandler`에 `Exception` 핸들러 추가 — 500 에러 스택 트레이스 콘솔 로그
- `ST_ADMIN_BASE`에 마스터 계정 생성 (`vibemaster` / ROLE_CD: SUPER)
- Redis 설치 및 실행 (`brew install redis && brew services start redis`)
- 어드민 사이드바 메뉴 구현 (feature/auth)
  - `GET /api/admin/system/menus` 연동 — TanStack Query
  - 대메뉴/소메뉴 아코디언 방식, Pretendard 폰트 적용
  - 현재 경로 활성화 스타일, 소메뉴 포함 대메뉴 자동 펼침
  - 단위 테스트 전체 통과 (36개)

## 진행 중
- 없음

## 다음 작업
- **상품 등록** 개발 시작 (`goods` 모듈)
  - 어드민 상품 등록 화면 (`/admin/products/new`)
  - 백엔드 `POST /api/admin/goods` API
  - 상품명, 가격, 재고, 이미지 등 기본 필드 포함 예정

## 메모
- `application-local.yml`의 DB URL이 `localhost:5432`로 되어 있어 접속 불가 — Supabase Transaction Pooler host(`aws-0-<region>.pooler.supabase.com:6543`)로 변경 필요
- `proxy.ts`가 `middleware.ts`로 명명되지 않아 Next.js 미들웨어로 동작하지 않음 — 추후 확인 필요
