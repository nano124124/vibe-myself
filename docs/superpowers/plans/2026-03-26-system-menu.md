# System Menu 조회 API Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어드민 메뉴를 DB로 관리하고 트리 구조로 조회하는 `GET /api/admin/system/menus` API를 구현한다.

**Architecture:** self-referencing 단일 테이블(`ST_MENU_BASE`)로 메뉴 계층을 관리한다. Mapper에서 전체 flat 조회 후 Service에서 트리로 조립한다. DB에 초기 메뉴 데이터를 하드코딩한다.

**Tech Stack:** Spring Boot 3, MyBatis, PostgreSQL, Lombok, JUnit 5 (Mockito)

**Spec:** `docs/superpowers/specs/2026-03-26-system-menu-design.md`

---

## Chunk 1: DB 설정

### Task 1: SQL 스크립트 작성

**Files:**
- Create: `backend/sql/menu_setup.sql`

- [ ] **Step 1: SQL 스크립트 작성**

`backend/sql/menu_setup.sql` 파일을 생성한다.

```sql
-- ST_MENU_BASE 테이블 생성
CREATE TABLE ST_MENU_BASE (
    MENU_NO         BIGINT          GENERATED BY DEFAULT AS IDENTITY,
    PARENT_MENU_NO  BIGINT          NULL,
    MENU_NM         VARCHAR(200)    NOT NULL,
    MENU_URL        VARCHAR(2000)   NULL,
    SORT_ORD        INTEGER         NOT NULL DEFAULT 0,
    USE_YN          CHAR(1)         NOT NULL DEFAULT 'Y',
    REG_DTM         TIMESTAMP       NOT NULL DEFAULT NOW(),
    REG_ID          VARCHAR(50)     NOT NULL,
    MOD_DTM         TIMESTAMP       NOT NULL DEFAULT NOW(),
    MOD_ID          VARCHAR(50)     NOT NULL,
    CONSTRAINT PK_ST_MENU_BASE PRIMARY KEY (MENU_NO),
    CONSTRAINT FK_ST_MENU_BASE_SELF FOREIGN KEY (PARENT_MENU_NO)
        REFERENCES ST_MENU_BASE (MENU_NO)
);

-- 초기 메뉴 데이터
INSERT INTO ST_MENU_BASE (MENU_NO, PARENT_MENU_NO, MENU_NM, MENU_URL, SORT_ORD, USE_YN, REG_ID, MOD_ID) VALUES
(1,  NULL, '대시보드',    '/admin/dashboard',       1, 'Y', 'SYSTEM', 'SYSTEM'),
(2,  NULL, '상품관리',    NULL,                     2, 'Y', 'SYSTEM', 'SYSTEM'),
(3,  NULL, '주문관리',    NULL,                     3, 'Y', 'SYSTEM', 'SYSTEM'),
(4,  NULL, '프로모션',    NULL,                     4, 'Y', 'SYSTEM', 'SYSTEM'),
(5,  NULL, '회원관리',    NULL,                     5, 'Y', 'SYSTEM', 'SYSTEM'),
(6,  NULL, '시스템관리',  NULL,                     6, 'Y', 'SYSTEM', 'SYSTEM'),
(11, 2,    '상품목록',    '/admin/products',         1, 'Y', 'SYSTEM', 'SYSTEM'),
(12, 3,    '주문목록',    '/admin/orders',           1, 'Y', 'SYSTEM', 'SYSTEM'),
(13, 3,    '클레임목록',  '/admin/claims',           2, 'Y', 'SYSTEM', 'SYSTEM'),
(14, 4,    '프로모션목록','/admin/promotions',       1, 'Y', 'SYSTEM', 'SYSTEM'),
(15, 4,    '이벤트목록',  '/admin/events',           2, 'Y', 'SYSTEM', 'SYSTEM'),
(16, 5,    '회원목록',    '/admin/members',          1, 'Y', 'SYSTEM', 'SYSTEM'),
(17, 6,    '메뉴관리',    '/admin/system/menus',     1, 'Y', 'SYSTEM', 'SYSTEM');

-- IDENTITY 시퀀스를 마지막 ID 이후로 재설정 (추후 INSERT 충돌 방지)
-- GENERATED BY DEFAULT AS IDENTITY는 pg_get_serial_sequence가 아닌 ALTER TABLE로 재설정
ALTER TABLE ST_MENU_BASE ALTER COLUMN MENU_NO RESTART WITH 100;
```

- [ ] **Step 2: DB에 직접 실행**

연결된 PostgreSQL에 접속해서 위 SQL을 실행한다.
`application-local.yml`의 DB 접속 정보를 사용한다.

- [ ] **Step 3: 데이터 확인**

```sql
SELECT MENU_NO, PARENT_MENU_NO, MENU_NM, MENU_URL, SORT_ORD FROM ST_MENU_BASE ORDER BY MENU_NO;
```

예상 결과: 13개 행 반환.

- [ ] **Step 4: Commit**

```bash
git add backend/sql/menu_setup.sql
git commit -m "chore: add ST_MENU_BASE DDL and seed data"
```

---

## Chunk 2: Model & Mapper

### Task 2: Menu 모델 클래스

**Files:**
- Create: `backend/src/main/java/com/vibemyself/model/system/Menu.java`

- [ ] **Step 1: Menu.java 작성**

```java
package com.vibemyself.model.system;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class Menu {
    private Long menuNo;
    private Long parentMenuNo;
    private String menuNm;
    private String menuUrl;
    private int sortOrd;
}
```

> `USE_YN`은 쿼리에서 필터링하므로 모델에 포함하지 않는다.

### Task 3: MenuMapper 인터페이스 + XML

**Files:**
- Create: `backend/src/main/java/com/vibemyself/mapper/system/MenuMapper.java`
- Create: `backend/src/main/resources/mapper/system/MenuMapper.xml`

- [ ] **Step 1: MenuMapper.java 작성**

```java
package com.vibemyself.mapper.system;

import com.vibemyself.model.system.Menu;
import org.apache.ibatis.annotations.Mapper;

import java.util.List;

@Mapper
public interface MenuMapper {
    List<Menu> selectAllActive();
}
```

- [ ] **Step 2: MenuMapper.xml 작성**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibemyself.mapper.system.MenuMapper">

    <resultMap id="menuResultMap" type="com.vibemyself.model.system.Menu">
        <id property="menuNo" column="MENU_NO"/>
        <result property="parentMenuNo" column="PARENT_MENU_NO"/>
        <result property="menuNm" column="MENU_NM"/>
        <result property="menuUrl" column="MENU_URL"/>
        <result property="sortOrd" column="SORT_ORD"/>
    </resultMap>

    <select id="selectAllActive" resultMap="menuResultMap">
        SELECT MENU_NO, PARENT_MENU_NO, MENU_NM, MENU_URL, SORT_ORD
        FROM ST_MENU_BASE
        WHERE USE_YN = 'Y'
        ORDER BY SORT_ORD
    </select>

</mapper>
```

- [ ] **Step 3: Commit**

```bash
git add backend/src/main/java/com/vibemyself/model/system/Menu.java \
        backend/src/main/java/com/vibemyself/mapper/system/MenuMapper.java \
        backend/src/main/resources/mapper/system/MenuMapper.xml
git commit -m "feat: add Menu model and MenuMapper"
```

---

## Chunk 3: DTO & Service (TDD)

### Task 4: MenuResponse DTO

**Files:**
- Create: `backend/src/main/java/com/vibemyself/dto/system/MenuResponse.java`

- [ ] **Step 1: MenuResponse.java 작성**

`children`은 Service에서 동적으로 추가되므로 `@Builder.Default`로 mutable list를 초기화한다.

```java
package com.vibemyself.dto.system;

import lombok.Builder;
import lombok.Getter;

import java.util.ArrayList;
import java.util.List;

@Getter
@Builder
public class MenuResponse {
    private Long menuNo;
    private String menuNm;
    private String menuUrl;
    private int sortOrd;

    @Builder.Default
    private List<MenuResponse> children = new ArrayList<>();
}
```

### Task 5: MenuService 단위 테스트 작성 (TDD - Red)

**Files:**
- Create: `backend/src/test/java/com/vibemyself/service/system/MenuServiceTest.java`

- [ ] **Step 1: 테스트 작성**

```java
package com.vibemyself.service.system;

import com.vibemyself.dto.system.MenuResponse;
import com.vibemyself.mapper.system.MenuMapper;
import com.vibemyself.model.system.Menu;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class MenuServiceTest {

    @Mock
    private MenuMapper menuMapper;

    @InjectMocks
    private MenuService menuService;

    @Test
    void 대메뉴와_소메뉴가_트리로_조립된다() {
        // given
        List<Menu> flatMenus = List.of(
                Menu.builder().menuNo(1L).parentMenuNo(null).menuNm("대시보드").menuUrl("/admin/dashboard").sortOrd(1).build(),
                Menu.builder().menuNo(2L).parentMenuNo(null).menuNm("상품관리").menuUrl(null).sortOrd(2).build(),
                Menu.builder().menuNo(11L).parentMenuNo(2L).menuNm("상품목록").menuUrl("/admin/products").sortOrd(1).build()
        );
        when(menuMapper.selectAllActive()).thenReturn(flatMenus);

        // when
        List<MenuResponse> result = menuService.getMenuTree();

        // then
        assertThat(result).hasSize(2);

        MenuResponse dashboard = result.get(0);
        assertThat(dashboard.getMenuNo()).isEqualTo(1L);
        assertThat(dashboard.getMenuNm()).isEqualTo("대시보드");
        assertThat(dashboard.getChildren()).isEmpty();

        MenuResponse goods = result.get(1);
        assertThat(goods.getMenuNo()).isEqualTo(2L);
        assertThat(goods.getChildren()).hasSize(1);
        assertThat(goods.getChildren().get(0).getMenuNm()).isEqualTo("상품목록");
    }

    @Test
    void 루트_메뉴는_sortOrd_기준으로_정렬된다() {
        // given — sortOrd가 3, 1, 2 순서로 섞여 있어도 1, 2, 3 순으로 정렬되어야 함
        List<Menu> flatMenus = List.of(
                Menu.builder().menuNo(3L).parentMenuNo(null).menuNm("주문관리").menuUrl(null).sortOrd(3).build(),
                Menu.builder().menuNo(1L).parentMenuNo(null).menuNm("대시보드").menuUrl("/admin/dashboard").sortOrd(1).build(),
                Menu.builder().menuNo(2L).parentMenuNo(null).menuNm("상품관리").menuUrl(null).sortOrd(2).build()
        );
        when(menuMapper.selectAllActive()).thenReturn(flatMenus);

        // when
        List<MenuResponse> result = menuService.getMenuTree();

        // then
        assertThat(result).hasSize(3);
        assertThat(result.get(0).getMenuNm()).isEqualTo("대시보드");
        assertThat(result.get(1).getMenuNm()).isEqualTo("상품관리");
        assertThat(result.get(2).getMenuNm()).isEqualTo("주문관리");
    }

    @Test
    void 소메뉴는_부모의_children에_sortOrd_기준으로_정렬된다() {
        // given
        List<Menu> flatMenus = List.of(
                Menu.builder().menuNo(3L).parentMenuNo(null).menuNm("주문관리").menuUrl(null).sortOrd(3).build(),
                Menu.builder().menuNo(13L).parentMenuNo(3L).menuNm("클레임목록").menuUrl("/admin/claims").sortOrd(2).build(),
                Menu.builder().menuNo(12L).parentMenuNo(3L).menuNm("주문목록").menuUrl("/admin/orders").sortOrd(1).build()
        );
        when(menuMapper.selectAllActive()).thenReturn(flatMenus);

        // when
        List<MenuResponse> result = menuService.getMenuTree();

        // then
        List<MenuResponse> children = result.get(0).getChildren();
        assertThat(children).hasSize(2);
        assertThat(children.get(0).getMenuNm()).isEqualTo("주문목록");
        assertThat(children.get(1).getMenuNm()).isEqualTo("클레임목록");
    }
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

`backend/` 디렉토리에서:
```bash
./gradlew test --tests "com.vibemyself.service.system.MenuServiceTest" 2>&1 | tail -20
```

예상: `MenuService` 클래스가 없으므로 컴파일 에러 또는 `FAILED`.

### Task 6: MenuService 구현 (TDD - Green)

**Files:**
- Create: `backend/src/main/java/com/vibemyself/service/system/MenuService.java`

- [ ] **Step 1: MenuService.java 작성**

```java
package com.vibemyself.service.system;

import com.vibemyself.dto.system.MenuResponse;
import com.vibemyself.mapper.system.MenuMapper;
import com.vibemyself.model.system.Menu;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

@Service
@RequiredArgsConstructor
public class MenuService {

    private final MenuMapper menuMapper;

    @Transactional(readOnly = true)
    public List<MenuResponse> getMenuTree() {
        List<Menu> flatMenus = menuMapper.selectAllActive();

        Map<Long, MenuResponse> index = new LinkedHashMap<>();
        for (Menu menu : flatMenus) {
            index.put(menu.getMenuNo(), MenuResponse.builder()
                    .menuNo(menu.getMenuNo())
                    .menuNm(menu.getMenuNm())
                    .menuUrl(menu.getMenuUrl())
                    .sortOrd(menu.getSortOrd())
                    .build());
        }

        List<MenuResponse> roots = new ArrayList<>();
        for (Menu menu : flatMenus) {
            MenuResponse response = index.get(menu.getMenuNo());
            if (menu.getParentMenuNo() == null) {
                roots.add(response);
            } else {
                MenuResponse parent = index.get(menu.getParentMenuNo());
                if (parent != null) {
                    parent.getChildren().add(response);
                }
            }
        }

        roots.sort(Comparator.comparingInt(MenuResponse::getSortOrd));
        roots.forEach(root -> root.getChildren().sort(Comparator.comparingInt(MenuResponse::getSortOrd)));

        return roots;
    }
}
```

- [ ] **Step 2: 테스트 실행 — 성공 확인**

```bash
./gradlew test --tests "com.vibemyself.service.system.MenuServiceTest" 2>&1 | tail -20
```

예상:
```
3 tests completed, 0 failed
BUILD SUCCESSFUL
```

- [ ] **Step 3: DTO + 테스트 커밋 (Red state)**

```bash
git add backend/src/main/java/com/vibemyself/dto/system/MenuResponse.java \
        backend/src/test/java/com/vibemyself/service/system/MenuServiceTest.java
git commit -m "test: add MenuService tree assembly tests"
```

- [ ] **Step 4: 구현 커밋 (Green state)**

```bash
git add backend/src/main/java/com/vibemyself/service/system/MenuService.java
git commit -m "feat: add MenuService with tree assembly logic"
```

---

## Chunk 4: Controller

### Task 7: MenuController 작성

**Files:**
- Create: `backend/src/main/java/com/vibemyself/controller/system/MenuController.java`

- [ ] **Step 1: MenuController.java 작성**

`AdminAuthController`가 이미 `/api/admin/system`을 사용 중이므로,
`MenuController`는 `/api/admin/system/menus`를 클래스 레벨로 선언한다.

```java
package com.vibemyself.controller.system;

import com.vibemyself.common.response.ApiResponse;
import com.vibemyself.dto.system.MenuResponse;
import com.vibemyself.service.system.MenuService;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
@RequestMapping("/api/admin/system/menus")
@RequiredArgsConstructor
public class MenuController {

    private final MenuService menuService;

    @GetMapping
    public ResponseEntity<ApiResponse<List<MenuResponse>>> getMenus() {
        return ResponseEntity.ok(ApiResponse.ok(menuService.getMenuTree()));
    }
}
```

- [ ] **Step 2: 서버 실행 확인**

`backend/` 디렉토리에서:
```bash
./gradlew bootRun
```

서버가 8080 포트에서 정상 실행되는지 확인한다.

- [ ] **Step 3: API 호출 확인**

먼저 어드민 로그인으로 쿠키를 얻은 후 메뉴 API를 호출한다.
비밀번호는 `application-local.yml` 또는 DB의 `ST_ADMIN_BASE` 테이블에서 확인한다.

```bash
# 로그인
curl -c /tmp/cookies.txt -s -X POST http://localhost:8080/api/admin/system/login \
  -H "Content-Type: application/json" \
  -d '{"loginId":"vibemaster","password":"<application-local.yml에서 확인>"}' | jq .

# 메뉴 조회
curl -b /tmp/cookies.txt -s http://localhost:8080/api/admin/system/menus | jq .
```

예상 응답:
```json
{
  "success": true,
  "data": [
    { "menuNo": 1, "menuNm": "대시보드", "menuUrl": "/admin/dashboard", "sortOrd": 1, "children": [] },
    { "menuNo": 2, "menuNm": "상품관리", "menuUrl": null, "sortOrd": 2,
      "children": [{ "menuNo": 11, "menuNm": "상품목록", "menuUrl": "/admin/products", "sortOrd": 1, "children": [] }]
    }
  ],
  "message": null
}
```

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/com/vibemyself/controller/system/MenuController.java
git commit -m "feat: add GET /api/admin/system/menus endpoint"
```
