spring:
  profiles:
    active: local
  datasource:
    # log4jdbc DriverSpy를 통해 실제 실행 SQL과 바인드 파라미터를 함께 로깅한다.
    url: jdbc:log4jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
    username: postgres.ekmmmbmsxwiprqzkrsmd
    password: Rjqnrdl12@@
    driver-class-name: org.postgresql.Driver
    hikari:
      initialization-fail-timeout: 0
      connection-test-query: SELECT 1
      maximum-pool-size: 3
      minimum-idle: 1

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.vibemyself.model
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
